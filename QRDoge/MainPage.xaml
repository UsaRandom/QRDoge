<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QRDoge.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

			<Image
                Source="dogedrawn.png"
                HeightRequest="185"
				Margin="0,30,0,0"
                Aspect="AspectFit"
                SemanticProperties.Description="dogecoin doge meme drawn" />

			<Label
                Text="QR-Doge"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

			<Button
                x:Name="ScanQRButton"
                Text="Scan QR" 
                SemanticProperties.Hint="Click to scan a dogecoin terminal qr code."
                Clicked="OnScanButtonClicked"
                HorizontalOptions="Fill" />

			<Button
                x:Name="NodeSettingsButton"
                Text="Node Connection Settings" 
                SemanticProperties.Hint="Click to send a transaction generated by a dogecoin terminal."
                Clicked="OnNodeSettingsButtonClicked"
                HorizontalOptions="Fill" />
		</VerticalStackLayout>
    </ScrollView>

</ContentPage>
